{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//": "Windows authentication, so this file carries no password and is safe to commit. To sign in as the SivayaanHMS SQL login instead, put that connection string in appsettings.Local.json (git-ignored) — see docs/SQL_SERVER_SETUP.md.",
    "Default": "Server=.\\SQLEXPRESS;Database=HMSLite;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=False;Application Name=SivayaanHMS"
  },
  "Cors": {
    "AllowedOrigins": ["http://localhost:5173"]
  },
  "Jwt": {
    "Key": "REPLACE-BEFORE-PRODUCTION-this-is-a-development-only-signing-key-32bytes+",
    "Issuer": "SivayaanHMS",
    "Audience": "SivayaanHMS",
    "ExpiryMinutes": 480
  }
}
